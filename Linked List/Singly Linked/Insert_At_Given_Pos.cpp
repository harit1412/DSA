#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
    int year;
    cin >> year;
    string s = to_string(year);
    if(year == 1918)
    {
        cout << "12.09." + s;
    }
    if()
    return 0;
}